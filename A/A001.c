#include <stdio.h>

int main(){
    //soma de dois numeros
    int x, y;
    printf("Digite dois numeros: ");
    scanf("%d%d", &x, &y);
    printf("A soma de %d e %d e %d\n", x, y, x+y);

    return 1;
}
